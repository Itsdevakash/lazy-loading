const Skeleton = () => {
  return (
    <div
      style={{
        height: "150px",
        background: "#e0e0e0",
        borderRadius: "8px",
        animation: "pulse 1.5s infinite"
      }}
    />
  );
};

export default Skeleton;
